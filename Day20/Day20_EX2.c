#include <stdio.h>

int main() {
	char a[] = "apple";
	char p[30] = "pine";

	int size_a = sizeof(a);
	printf("%d", size_a);

	/*
	for (int i = 0; i < 30; i++) {
		
	}

	printf("%s", p); 
	*/

	return 0;
}